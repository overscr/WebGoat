=== Let's try
Click the "log in" button to send a login request.
Then, try using a packet sniffer to intercept the request and to read the credentials of that user from it.
The request no longer carries any credentials, they are verified on the server, so there is nothing left to capture and replay in the fields below.
